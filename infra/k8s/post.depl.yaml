apiVersion: apps/v1
kind: Deployment
metadata:
  name: posts-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: posts
  template:
    metadata:
      labels:
        app: posts
    spec:
      containers:
        - name: posts
          image: razmjo/posts

---
apiVersion: v1
kind: Service
metadata:
  name: post-clusterip-service
spec:
  type: ClusterIP
  selector:
    app: post
  ports:
    - name: posts
      protocol: TCP
      port: 4000
      targetPort: 4000
  